apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: modelroutes.modelplane.ai
spec:
  group: modelplane.ai
  names:
    categories: [crossplane, modelplane, models]
    kind: ModelRoute
    plural: modelroutes
    shortNames: [mrt]
  scope: Namespaced
  versions:
  - name: v1alpha1
    served: true
    referenceable: true
    additionalPrinterColumns:
    - name: SERVICE
      type: string
      jsonPath: .spec.serviceName
    - name: GATEWAY
      type: string
      jsonPath: .spec.gatewayName
    - name: ADDRESS
      type: string
      jsonPath: .status.address
    - name: ENDPOINTS
      type: integer
      jsonPath: .status.endpoints.ready
    schema:
      openAPIV3Schema:
        description: >-
          A ModelRoute is one ModelService's routing on one InferenceGateway: the
          AIGatewayRoute matching the service's model name, plus, per endpoint,
          the Backend, credential and policy the gateway needs to reach it. The
          ModelService composes one per gateway that serves it, pinned to that
          gateway, and this renders onto the gateway's cluster.

          It is machine-generated. `kubectl get modelroutes -l
          modelplane.ai/service=<name>` is the per-gateway view of where a service
          is served and whether each gateway is carrying it.
        type: object
        required: [spec]
        properties:
          spec:
            type: object
            # A ModelRoute is machine-generated by the ModelService compose
            # function. The API server still serves and accepts it, so requiring
            # these lets the consuming function trust the resolved shape rather
            # than re-checking it.
            required: [gatewayName, serviceName, endpoints, timeouts]
            properties:
              gatewayName:
                type: string
                minLength: 1
                maxLength: 253
                description: >-
                  Name of the InferenceGateway this route is pinned to. The
                  function resolves the gateway's cluster, its ProviderConfig and
                  its address from here, and renders onto that cluster.
              serviceName:
                type: string
                minLength: 1
                maxLength: 253
                description: >-
                  Name of the ModelService this route belongs to. With the
                  ModelRoute's own namespace it derives the composed object names
                  and the model name a caller passes (<namespace>/<service>), so
                  the service and its routes agree on both.
              endpoints:
                type: array
                description: >-
                  A priority order over ModelEndpoints, each entry selecting a
                  set of them by label. A verbatim copy of the ModelService's
                  spec.endpoints.
                minItems: 1
                maxItems: 32
                x-kubernetes-list-type: map
                x-kubernetes-list-map-keys: [name]
                items:
                  type: object
                  required: [name, selector]
                  properties:
                    name:
                      type: string
                      description: A stable name for this entry, unique within the service.
                      minLength: 1
                      maxLength: 63
                      pattern: '^[a-z0-9]([-a-z0-9]*[a-z0-9])?$'
                    priority:
                      type: integer
                      description: >-
                        Lower is preferred. Entries at the same priority share
                        traffic by weight. A higher-numbered entry takes a growing
                        share of traffic as lower-numbered ones lose healthy
                        endpoints, and takes over entirely once they have none.
                      format: int32
                      minimum: 0
                      maximum: 63
                      default: 0
                    weight:
                      type: integer
                      description: >-
                        Share of traffic for this entry relative to the other
                        entries at the same priority, spread as evenly as
                        possible across the endpoints it matches.
                      format: int32
                      minimum: 1
                      maximum: 1000000
                      default: 1
                    selector:
                      type: object
                      description: >-
                        Selects ModelEndpoints in this route's namespace.
                      required: [matchLabels]
                      properties:
                        matchLabels:
                          type: object
                          additionalProperties:
                            type: string
                            maxLength: 63
                          minProperties: 1
                          maxProperties: 16
              timeouts:
                type: object
                description: >-
                  How long the gateway waits on this service's endpoints. A
                  verbatim copy of the ModelService's spec.timeouts.
                required: [request, idle]
                properties:
                  request:
                    type: string
                    description: How long a request may take end to end, including retries.
                    pattern: '^([0-9]{1,5}(h|m|s|ms)){1,4}$'
                  idle:
                    type: string
                    description: >-
                      How long an endpoint may send nothing. Before its first
                      byte the gateway retries the request, on another endpoint if
                      there is one. After it the stream is cut short. 0s
                      disables it.
                    pattern: '^([0-9]{1,5}(h|m|s|ms)){1,4}$'
          status:
            type: object
            properties:
              model:
                type: string
                description: >-
                  The name a caller passes as the request's model,
                  <namespace>/<service>.
              address:
                type: string
                description: External address of the gateway this route is on.
              endpoints:
                type: object
                description: >-
                  Observed endpoint counts for this route, across all priorities.
                properties:
                  total:
                    type: integer
                    format: int32
                    minimum: 0
                  ready:
                    type: integer
                    format: int32
                    minimum: 0
              conditions:
                type: array
                items:
                  type: object
