apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: modelplacements.modelplane.ai
spec:
  compositeTypeRef:
    apiVersion: modelplane.ai/v1alpha1
    kind: ModelPlacement
  mode: Pipeline
  pipeline:
  - functionRef:
      name: modelplane-modelplanecompose-model-placement
    step: compose-model-placement
