apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: inferencegateways.modelplane.ai
spec:
  compositeTypeRef:
    apiVersion: modelplane.ai/v1alpha1
    kind: InferenceGateway
  mode: Pipeline
  pipeline:
  - functionRef:
      name: modelplane-modelplanecompose-inference-gateway
    step: compose-inference-gateway
