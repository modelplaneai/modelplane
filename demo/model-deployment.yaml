# Deploy the model to both inference environments.
# The ML team applies this — Modelplane handles the rest.
apiVersion: modelplane.ai/v1alpha1
kind: ModelDeployment
metadata:
  name: qwen-demo
  namespace: ml-team
spec:
  modelRef:
    name: qwen-0.5b-vllm
  environments: 2
