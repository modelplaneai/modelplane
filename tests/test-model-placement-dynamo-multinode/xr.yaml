apiVersion: modelplane.ai/v1alpha1
kind: ModelPlacement
metadata:
  name: llama405b-dynamo-cluster
  namespace: ml-team
spec:
  modelRef:
    name: llama-405b
  inferenceEnvironmentRef:
    name: dynamo-cluster
  scaling:
    signal: Fixed
    fixed:
      replicas: 1
